{
  "devDependencies": {
    "@tsconfig/recommended": "^1.0.1",
    "prettier": "^2.6.2",
    "typescript": "^4.6.4"
  },
  "private": true,
  "scripts": {
    "build": "tsc",
    "fix": "run-s fix:*",
    "fix:prettier": "prettier --write index.ts",
    "fixpack": "npx fixpack",
    "lint": "run-s lint:*",
    "lint:prettier": "prettier --check index.ts",
    "lint:tsc": "tsc --noEmit",
    "watch": "tsc --watch"
  }
}
